export type ConstructionService = {
  title: string;
  description: string;
};

export type ConstructionProject = {
  title: string;
  category: string;
  scope: string;
  image: string;
};

export type ConstructionCoreValue = {
  title: string;
  description: string;
};
